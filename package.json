{
  "name": "auther",
  "version": "1.0.0",
  "description": "Queens & Gods — reader site with purchase-gated access",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev . --project-name=queens-gods",
    "deploy": "wrangler pages deploy . --project-name=queens-gods",
    "test": "node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "stripe": "^14.10.0"
  },
  "devDependencies": {
    "wrangler": "^4.67.0"
  }
}
